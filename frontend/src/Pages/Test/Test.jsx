const Test = () => {
  return (
    <div
      style={{ backgroundColor: "#F7F7FC", overflowX: "hidden" }}
      className="py-5"
    >
      {/* <div
        className={`${styles.badge} ${
          pack.isMostPopular
            ? styles.main_bg
            : pack.isBestOffer
            ? styles.offer
            : styles.custom_badge
        } ${isArLang ? "me-auto" : "ms-auto"}`}
      >
        <span>
          {pack.isMostPopular
            ? key("mostPopular")
            : pack.isBestOffer
            ? key("deal")
            : key("cust")}
        </span>
      </div> */}
    </div>
  );
};

export default Test;

const Test = () => {
  return (
    <div
      style={{ backgroundColor: "#F7F7FC", overflowX: "hidden" }}
      className="py-5"
    >
      {/* <td>{`${
                          item.amount
                        } ${item.category === "expense" ? "-" : ""}`}</td> */}
    </div>
  );
};

export default Test;
